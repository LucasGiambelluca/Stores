import React from 'react';
import { PageBuilder } from './PageBuilder';

export const AdminAboutEditor: React.FC = () => {
  return (
    <PageBuilder 
      title="Editor de About Us"
      configKey="about"
    />
  );
};
